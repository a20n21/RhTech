name: CI Pipeline

on:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: matias1210/rhtech-backend:${{ github.sha }}

      - name: Update Kubernetes Manifest
        if: github.event_name == 'push' && !contains(github.event.head_commit.message, 'chore: update backend image')
        run: |
          sed -i "s|image: matias1210/rhtech-backend:.*|image: matias1210/rhtech-backend:${{ github.sha }}|g" k8s/base/backend/deployment.yaml
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add k8s/base/backend/deployment.yaml
          git commit -m "chore: update backend image to ${{ github.sha }}"
          git push
